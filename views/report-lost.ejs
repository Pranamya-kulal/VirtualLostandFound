<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Report Lost Item</title>
    <style>
        /* Global Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Body Styling */
        body {
            font-family: 'Arial', sans-serif;
            background-color: #2C3E50; /* Blue background */
            color: white; /* Text color change to white */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            padding: 0 20px;
        }

        /* Form Container */
        .form-container {
            background-color: rgba(255, 255, 255, 0.8); /* Semi-transparent white */
            box-shadow: 0px 4px 12px rgba(0, 0, 0, 0.1);
            padding: 30px;
            border-radius: 12px;
            max-width: 600px;
            width: 100%;
            text-align: center;
            display: flex;
            flex-direction: column;
            gap: 20px;
            border-left: 5px solid #3498DB;
            border-right: 5px solid #3498DB;
        }

        h1 {
            font-size: 2.5rem;
            color: #2980B9; /* Blue color for header */
            margin-bottom: 20px;
            font-weight: bold;
        }

        /* Error message styling */
        .error-message {
            background-color: #E74C3C;
            color: white;
            padding: 12px;
            border-radius: 8px;
            font-size: 1.1rem;
        }

        /* Input Fields */
        input[type="text"],
        input[type="file"],
        textarea {
            width: 100%;
            padding: 12px;
            margin-top: 8px;
            margin-bottom: 15px;
            border: 1px solid #BDC3C7;
            border-radius: 8px;
            font-size: 1rem;
            transition: border-color 0.3s ease;
        }

        input[type="text"]:focus,
        input[type="file"]:focus,
        textarea:focus {
            border-color: #3498DB;
            outline: none;
        }

        /* Submit Button */
        button {
            background-color: #2ECC71;
            color: white;
            padding: 15px 25px;
            border: none;
            border-radius: 8px;
            font-size: 1.2rem;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #27AE60;
        }

        /* Link Styling */
        a {
            color: #3498DB; /* Blue color for link */
            font-size: 1rem;
            text-decoration: none;
            margin-top: 20px;
            display: inline-block;
            transition: color 0.3s ease;
        }

        a:hover {
            color: #1A5276;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .form-container {
                padding: 20px;
            }
        }

    </style>
</head>
<body>

    <div class="form-container">
        <h1>Report a Lost Item</h1>

        <!-- Error Message -->
        <% if (errorMessage) { %>
            <div class="error-message">
                <%= errorMessage %>
            </div>
        <% } %>

        <!-- Form -->
        <form action="/submit-lost-item" method="POST" enctype="multipart/form-data">
            <div>
                <label for="item_name">Item Name:</label>
                <input type="text" name="item_name" id="item_name" value="<%= item_name %>" required placeholder="Enter item name">
            </div>

            <div>
                <label for="item_description">Item Description:</label>
                <textarea name="item_description" id="item_description" required placeholder="Provide a detailed description of the item"><%= item_description %></textarea>
            </div>

            <div>
                <label for="item_picture">Upload Picture:</label>
                <input type="file" name="item_picture" id="item_picture" required>
            </div>

            <div>
                <button type="submit">Submit</button>
            </div>
        </form>

        <!-- Back to Home Link -->
        <a href="/home">Back to Home</a>
    </div>

</body>
</html>
